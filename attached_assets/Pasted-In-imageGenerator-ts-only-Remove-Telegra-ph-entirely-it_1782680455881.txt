In imageGenerator.ts only. Remove Telegra.ph entirely — it is unreliable and unnecessary. Instead of uploading the image buffer to Telegra.ph, return the Pollinations URL directly.
Current broken flow:

Pollinations URL → fetch → buffer → upload to Telegra.ph → get URL → return
New correct flow:

Pollinations URL → return URL directly (no upload needed, Telegram can fetch images from any public URL)
Rewrite tryPollinations to return the URL directly:
typescriptasync function tryPollinations(
  characterName: string,
  stylePrefix: string,
  subGenres: string[],
  imageSeed: number
): Promise<string | null> {
  try {
    const parts = [characterName, stylePrefix, ...subGenres].filter(Boolean);
    const cleanPrompt = parts.join(', ').replace(/,\s*$/, '').trim();
    const encodedPrompt = encodeURIComponent(cleanPrompt);
    const url = `https://image.pollinations.ai/prompt/${encodedPrompt}?model=flux&width=512&height=512&nologo=true&seed=${imageSeed}`;
    
    console.log('Image prompt:', cleanPrompt);
    console.log('Pollinations URL:', url);

    // Verify the URL is reachable by doing a HEAD request first
    const check = await fetch(url, {
      method: 'HEAD',
      headers: { 'Referer': 'https://pollinations.ai' },
      signal: AbortSignal.timeout(65000)
    });

    console.log('Pollinations status:', check.status);

    if (check.ok) {
      console.log('Pollinations success — returning URL directly');
      return url;
    }

    console.log('Pollinations check failed:', check.status);
    return null;
  } catch (err: any) {
    console.log('Pollinations error:', err?.message);
    return null;
  }
}
Remove completely:

uploadToTelegraph function — delete entirely
All axios imports related to Telegra.ph
All FormData uploads to telegra.ph
All references to telegra.ph anywhere in the file

Fallback chain becomes:

Pollinations with character prompt → return URL directly
Pollinations with portrait, detailed face fallback prompt → return URL directly
avatar_url from character record → return as-is
Dicebear placeholder → https://api.dicebear.com/7.x/bottts/svg?seed=${characterName}

On success log:
typescriptconsole.log('Image URL returned:', url);
Do not touch any other files.