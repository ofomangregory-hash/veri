Please open 'imageGenerator.ts' (or our active prompt factory file). 

We need to rewrite our prompt building string template and the final URL query constructor to force Pollinations to act like a premium 'Pony V7' model cluster, optimizing our look for the clean vector aesthetic used by apps like Lucid Dreams. 

Please modify ONLY the template variables, string logic, and URL constructor in this file—do not touch any surrounding database logic or application routes:

1. Enable 100% Uncensored Payload Mapping (The Dynamic Variables):
- Ensure the script extracts custom physical parameters directly from our Supabase character object (e.g., character.name, character.age, character.breastSize, character.style, character.clothing).
- Do not sanitize or strip out mature description descriptors. Construct the dynamic core text block like this: "${character.physicalTraits}, ${character.breastSize} chest, wearing ${character.clothing || 'sensual attire'}".

2. Inject Premium High-Weight Quality Score Tags (The Positive Prompt):
Prepend the exact quality-forcing score weights used by premium roleplay platforms to the front of every prompt string to clean up the line-art and backgrounds:
"score_9, score_8_up, score_7_up, source_anime, rating_explicit, ultra-detailed 2D anime cel-shading style, crisp heavy black lineart outlines, 100% flat digital coloring, vibrant vector art, smooth digital color gradients, minimalist clean room interior background, sharp focus, digital illustration, modern anime aesthetic, masterpiece artwork"

3. Enforce Half-Body / Full-Body Portrait Framing:
Append strict camera angle tags to pull the lens back and stop the engine from rendering tight facial crops:
"either full-body shot or half-body shot composition, waist-up portrait perspective, showing complete clothing and legs context, well-proportioned perfect anatomy"

4. Build a Heavy-Duty Texture Filter (The Negative Prompt):
Create a strict negative prompt string constant to aggressively strip out smudged oil textures and realistic elements:
const NEGATIVE_PROMPT = "score_4, score_5, score_6, lowres, bad anatomy, deformed limbs, face zoom, close-up, headshot, oil painting, canvas texture, watercolor smudge, airbrushed skin, realistic skin pores, photorealism, blurry lines, heavy brushes, dark muddy shadows, messy sketch";

5. Upgrade the Pollinations URL to the Flux Architecture (The Endpoint Builder):
Rewrite the final URL builder function to explicitly switch the target engine to 'flux' (which handles high-detail styling and uncensored elements flawlessly), set enhance=false so it does not overwrite our score tags, and append the negative modifier parameter block safely:
const finalUrl = `https://pollinations.ai{encodeURIComponent(finalPrompt)}?width=512&height=768&nologo=true&enhance=false&model=flux&safe=false&negative=${encodeURIComponent(NEGATIVE_PROMPT)}`;

Please apply these precise parameter updates and engine configurations to our prompt builder code now.
