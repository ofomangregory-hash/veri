{
  "name": "z-fantasy-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=3001 vite --config vite.config.ts --host 0.0.0.0",
    "build": "BASE_PATH=/ vite build --config vite.config.ts",
    "serve": "vite preview --config vite.config.ts --host 0.0.0.0",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
